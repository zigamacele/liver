export enum ButtonVariants {
  default = 'default',
  destructive = 'destructive',
  outline = 'outline',
  secondary = 'secondary',
  ghost = 'ghost',
  link = 'link',
}

export enum ButtonSize {
  default = 'default',
  sm = 'sm',
  lg = 'lg',
  icon = 'icon',
}
